name: ci

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: test (py${{ matrix.python }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python: ["3.10", "3.11", "3.12"]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev,privacy,peft]"

      - name: Lint with ruff
        run: ruff check chorus tests benchmarks

      - name: Run tests with pytest
        run: pytest tests/ -v --tb=short -m "not network" -k "not test_runner_run_produces_report_with_both_strategies"

      - name: Smoke-check eval harness wiring
        run: chorus eval --config benchmarks/configs/smoke.yaml --check-only
